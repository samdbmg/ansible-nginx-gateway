---
galaxy_info:
  author: Sam M-G
  description: Sets up NGINX to act as a gateway for static sites and reverse proxying

  license: Apache

  min_ansible_version: 2.5

  #
  # platforms is a list of platforms, and each platform has a name and a list of versions.
  #
  platforms:
    - name: Ubuntu
      versions:
        - bionic
        - focal

  galaxy_tags:
    - nginx
    - proxy
    - mtls
    - certificates

dependencies: []
